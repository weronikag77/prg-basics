# Define an anonymous function is_even(number) that checks whether a number is even. 
# Then test it by writing a program.

is_even = lambda number: True if number%2 == 0 else False

print(is_even(7))