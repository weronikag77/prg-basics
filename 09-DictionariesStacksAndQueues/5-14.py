# Use the queue. Each new customer receives a ticket with an 
# automatically assigned consecutive number and is added to the end of the queue. 
# The next customer to be served is taken from the beginning of the queue.
import queue

line = queue.Queue()

